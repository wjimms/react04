




const initialState = {
    users: [],
    count: 10,
    allEmployees: false
}


export default(state = initialState,action) => {
    switch(action.type) {
        default: return state
    }
}

